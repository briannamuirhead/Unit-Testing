 /**
* This is a function that adds two arrays
* @param {Array} array1 is the first array to add
* @param {Array} array2 is the second array that gets added to array1
* @return {Array} the sum of the two arrays
*/
function addTwoArrays (array1, array2) {
    return [];
}
